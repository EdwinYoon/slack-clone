type RegisterResponse {
  errors: [Error!]
  approved: Boolean
}

type Mutation {
  # registerToTeam(email: String!, password: String!, teamId: String!): RegisterResponse!
  registerToTeam(email: String!, password: String!): RegisterResponse!
}