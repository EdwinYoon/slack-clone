type SendMessageResponse {
  approved: Boolean
  errors: [Error!]
  message: Message
}

type Mutation {
  sendMessage(text: String!, userId: String!, teamId: String!, channelId: String!): SendMessageResponse!
}
